{
    "http-basic": {
        "registry.redhat.io": {
            "username": "{{redhat_uname}}",
            "password": "{{redhat_pword}}"
        },
        "{{custom_registry_domain}}": {
            "username": "{{push_username}}",
            "password": "{{push_password}}"
        }
    }
}